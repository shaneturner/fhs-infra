# Forrest Hill Infrastructure - CLI Instructions

## Running Commands from the Host
To run Craft or Composer commands without entering the container, use `docker exec` from the host:

### Craft CLI
docker exec fhs-craft-app php craft <command>

### Composer
docker exec fhs-craft-app composer <command>

## Entering the Container (Interactive Shell)
To open an interactive shell inside the Craft application container:

docker exec -it fhs-craft-app sh

## Submodule Synchronization
To pull the latest changes from all subprojects and update the root repository pointer:

./scripts/sync-submodules.sh

## Production Operations

### Deploy / Update Production
1. Pull the latest code: `git pull`
2. Restart and rebuild: `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build`

### Reload Caddy Configuration
To apply Caddyfile changes without a full container restart:

docker exec fhs-caddy caddy reload --config /etc/caddy/Caddyfile

### Performance & Cache Status
To verify the health of the production cache:

curl -s -I https://forresthill.school.nz | grep -i 'cache-status'
